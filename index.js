// alert ('Hello');
// console.log();

let firstOperad = Number(prompt("введіть перше число: ")); // 5
let secondOperand = Number(prompt("Введіть друге число")); // 10
let result = firstOperad + secondOperand;

if(Number .isNaN(result) === true) {
  console.log('Ти не правий, введ число');
}else {
  console.log(result);
}
